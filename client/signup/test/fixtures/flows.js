const exported = {
	main: {
		steps: [ 'user', 'site' ],
		destination: '/',
	},

	account: {
		steps: [ 'user', 'site' ],
		destination: '/',
	},

	other: {
		steps: [ 'user', 'site' ],
		destination: '/',
	},

	filtered: {
		steps: [ 'user', 'site' ],
		destination: '/',
	},
};

export default exported;

export const { main, account, other, filtered } = exported;
